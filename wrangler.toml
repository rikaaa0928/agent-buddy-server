name = "agent-buddy-server"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# 启用 Cloudflare 智能放置 (Smart Placement):
# 自动将 Worker 调度到离上游服务 (OpenAI / Google / Anthropic) 最近的机房 (美国机房) 执行，
# 避免因 Worker 在香港/亚洲机房运行而触发 OpenAI 的地区封锁 403 错误！
[placement]
mode = "smart"

# [vars]
# 提示: MANAGEMENT_KEY 完全无需在此修改！
# - 方式一（零配置）：若未设置环境变量，服务首次运行会自动生成一个高强度安全密钥并存入 KV，网页端首次打开会自动完成登录并展示给你复制。
# - 方式二（环境变量）：可在 Cloudflare 网页后台 (Settings -> Variables and Secrets) 直接添加 MANAGEMENT_KEY 变量，无需修改代码文件。
# MANAGEMENT_KEY = "your-custom-key"

# KV namespace 用于持久化保存 OAuth 凭据:
# - 方式一（推荐，网页控制台从 GitHub 部署）：保持下方注释即可！部署后直接在 Cloudflare 网页后台 (Settings -> Bindings -> Add KV Namespace) 绑定 AUTH_KV 即可。
# - 方式二（CLI 命令行部署）：取消下方注释，将 id 改为您运行 `npx wrangler kv namespace create AUTH_KV` 生成的真实 ID。
# [[kv_namespaces]]
# binding = "AUTH_KV"
# id = "auth_kv_placeholder"
